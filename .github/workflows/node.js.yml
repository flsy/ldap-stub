name: Build and Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: |
#          yarn build
          VERSION=$(github.run_id)


#    steps:
#      - uses: actions/checkout@v2
#      - run: yarn
#      #      - run: yarn prettier
#      #      - run: yarn lint
#      - run: yarn build
#      - run: yarn jest --forceExit
